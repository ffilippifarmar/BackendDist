const _isValidRegister = require('./_isValidRegistrer');
const _isValidLogin = require('./_isValidLogin');
const _isValidContact = require('./_isValidContact');

module.exports = {
    _isValidRegister,
    _isValidLogin,
    _isValidContact
}